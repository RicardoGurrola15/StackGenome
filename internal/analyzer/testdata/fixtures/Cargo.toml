[package]
name = "my-cli-tool"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
tokio = { version = "1.32.0", features = ["full"] }
clap = "4.4.0"

[dev-dependencies]
assert_cmd = "2.0.12"
