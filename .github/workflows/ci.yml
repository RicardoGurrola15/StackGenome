name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test-go:
    name: Go Lint, Test, and VulnCheck
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.22.x'
          cache: true

      - name: Verify dependencies
        run: go mod verify

      - name: Format Check
        run: if [ -n "$(go fmt ./...)" ]; then echo "Code not formatted"; exit 1; fi

      - name: Vet
        run: go vet ./...

      - name: Run Tests (Race)
        run: go test -v -race ./...

      - name: Install govulncheck
        run: go install golang.org/x/vuln/cmd/govulncheck@latest

      - name: Run govulncheck
        run: govulncheck ./...

  test-backend:
    name: Backend Lint and Test
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./backend
    env:
      # Prevent npm from using any macOS-style /Volumes path for cache
      npm_config_cache: /tmp/npm-cache
    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          # Do not use built-in cache here — we set npm_config_cache manually above
          # to avoid npm picking up local /Volumes paths from lockfile metadata

      - name: Install dependencies
        run: npm ci --prefer-offline

      - name: Type Check
        run: npx tsc --noEmit

      - name: Run Vitest
        run: npm test

  test-web:
    name: Web Build and Type Check
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./web
    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: './web/package-lock.json'

      - name: Install dependencies
        run: npm ci

      - name: Type Check
        run: npx tsc --noEmit

      - name: Build (static export)
        run: npm run build
